<form method="post" action="{{ path('app_s_a_v_delete', {'id': s_a_v.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ s_a_v.id) }}">
    <button class="btn">Delete</button>
</form>
